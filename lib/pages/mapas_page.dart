import 'package:flutter/material.dart';

import '../widgets/scans_titles.dart';

class MapasPage extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
     return ScanTitles(tipo: 'geo');
    
  }
}
